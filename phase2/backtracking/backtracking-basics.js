/**
 * TASK 1: Print all binary strings of length N.
 * Example: N = 2 -> ["00","01","10","11"]
 */
function task1(n = 2) {}
console.log(task1());

/**
 * TASK 2: Generate all subsets of an array.
 * Example: [1,2] -> [[],[1],[2],[1,2]]
 */
function task2(arr = [1, 2]) {}
console.log(task2());

/**
 * TASK 3: Generate all permutations of a string.
 * Example: "ab" -> ["ab","ba"]
 */
function task3(str = "ab") {}
console.log(task3());

/**
 * TASK 4: Generate all combinations of a string.
 * Example: "abc" -> ["a","b","c","ab","ac","bc","abc"]
 */
function task4(str = "abc") {}
console.log(task4());

/**
 * TASK 5: Print all paths in a 2x2 grid from top-left to bottom-right.
 * Moves allowed: Right (R), Down (D)
 * Output: ["RD","DR"]
 */
function task5() {}
console.log(task5());

/**
 * TASK 6: Generate all valid parentheses combinations for N pairs.
 * Example: N = 2 -> ["()()","(())"]
 */
function task6(n = 2) {}
console.log(task6());

/**
 * TASK 7: Generate all possible letter combinations from digits.
 * Example: "23" -> ["ad","ae","af","bd","be","bf","cd","ce","cf"]
 */
function task7(digits = "23") {}
console.log(task7());

/**
 * TASK 8: Find all subsets whose sum equals target.
 * Example: [1,2,3], target = 3 -> [[1,2],[3]]
 */
function task8(arr = [1, 2, 3], target = 3) {}
console.log(task8());

/**
 * TASK 9: Generate all permutations of an array.
 * Example: [1,2,3]
 */
function task9(arr = [1, 2, 3]) {}
console.log(task9());

/**
 * TASK 10: Solve simple maze paths.
 * 0 = blocked, 1 = open
 * Return all paths from top-left to bottom-right.
 */
function task10() {}
console.log(task10());
