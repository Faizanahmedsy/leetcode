/**
 * TASK 1: Implement a stack using an array.
 * - Create an empty stack
 * - Push 3 elements
 * - Pop 1 element
 * - Return the final stack
 */
function task1() {}
console.log(task1());

/**
 * TASK 2: Check if a string has valid parentheses.
 * Only '(' and ')' are present.
 * Return true or false.
 * Example: "()()" -> true, "((" -> false
 */
function task2() {}
console.log(task2());

/**
 * TASK 3: Reverse a string using a stack.
 * Example: "abc" -> "cba"
 */
function task3() {}
console.log(task3());

/**
 * TASK 4: Given an array, return the next greater element for each item.
 * Example: [2,1,2] -> [null,2,null]
 * (Use stack logic, not nested loops)
 */
function task4() {}
console.log(task4());

/**
 * TASK 5: Remove adjacent duplicate characters using a stack.
 * Example: "abbaca" -> "ca"
 */
function task5() {}
console.log(task5());
