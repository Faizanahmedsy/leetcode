/**
 * TASK 1: Print numbers from 1 to N using recursion.
 * Example: N = 5 -> 1 2 3 4 5
 */
function task1(n = 5) {}
console.log(task1());

/**
 * TASK 2: Calculate factorial of a number using recursion.
 * Example: 5 -> 120
 */
function task2(n = 5) {}
console.log(task2());

/**
 * TASK 3: Calculate sum of first N natural numbers using recursion.
 * Example: 5 -> 15
 */
function task3(n = 5) {}
console.log(task3());

/**
 * TASK 4: Reverse a string using recursion.
 * Example: "abc" -> "cba"
 */
function task4(str = "abc") {}
console.log(task4());

/**
 * TASK 5: Check if a string is a palindrome using recursion.
 * Example: "madam" -> true
 */
function task5(str = "madam") {}
console.log(task5());

/**
 * TASK 6: Find the maximum element in an array using recursion.
 * Example: [1, 5, 3] -> 5
 */
function task6(arr = [1, 5, 3]) {}
console.log(task6());

/**
 * TASK 7: Count number of digits in a number using recursion.
 * Example: 1234 -> 4
 */
function task7(num = 1234) {}
console.log(task7());

/**
 * TASK 8: Calculate power (a^b) using recursion.
 * Example: 2^3 -> 8
 */
function task8(a = 2, b = 3) {}
console.log(task8());

/**
 * TASK 9: Print elements of an array using recursion.
 * Example: [1,2,3] -> 1 2 3
 */
function task9(arr = [1, 2, 3], index = 0) {}
console.log(task9());

/**
 * TASK 10: Find sum of array elements using recursion.
 * Example: [1,2,3] -> 6
 */
function task10(arr = [1, 2, 3], index = 0) {}
console.log(task10());
