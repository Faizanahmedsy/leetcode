/**
 * ARRAY PREREQUISITES – UNSOLVED
 *
 * Array = continuous memory, index-based access
 *
 * Time Complexity:
 * Access: O(1)
 * Traversal: O(n)
 *
 * Used in:
 * - Almost every DSA problem
 * - Two pointers
 * - Sliding window
 * - Prefix sums
 */

/**
 * TASK 1: Traverse array and print elements
 * Example: [1,2,3]
 */
function task1() {
  const arr = [1, 2, 3];
}
console.log("Task 1:", task1());

/**
 * TASK 2: Accumulator pattern (sum of array)
 * Example: [1,2,3] → 6
 */
function task2() {
  const arr = [1, 2, 3];
}
console.log("Task 2:", task2());

/**
 * TASK 3: Accumulator pattern (find max)
 * Example: [3,1,4,2] → 4
 */
function task3() {
  const arr = [3, 1, 4, 2];
}
console.log("Task 3:", task3());

/**
 * TASK 4: Build new array (double values)
 * Example: [1,2,3] → [2,4,6]
 */
function task4() {
  const arr = [1, 2, 3];
}
console.log("Task 4:", task4());

/**
 * TASK 5: Filter elements
 * Example: [1,2,3,4] → [2,4]
 */
function task5() {
  const arr = [1, 2, 3, 4];
}
console.log("Task 5:", task5());

/**
 * TASK 6: Two pointers (sum of pairs)
 * Example: [1,2,3,4], target=5 → true
 */
function task6(target = 5) {
  const arr = [1, 2, 3, 4];
}
console.log("Task 6:", task6());

/**
 * TASK 7: Reverse array in-place
 * Example: [1,2,3] → [3,2,1]
 */
function task7() {
  const arr = [1, 2, 3];
}
console.log("Task 7:", task7());

/**
 * TASK 8: Count occurrences
 * Example: [1,1,2,3,1] → count of 1 = 3
 */
function task8() {
  const arr = [1, 1, 2, 3, 1];
}
console.log("Task 8:", task8());

/**
 * TASK 9: Prefix sum
 * Example: [1,2,3] → [1,3,6]
 */
function task9() {
  const arr = [1, 2, 3];
}
console.log("Task 9:", task9());

/**
 * TASK 10: Find missing number (0 to n)
 * Example: [0,1,3] → 2
 */
function task10() {
  const arr = [0, 1, 3];
}
console.log("Task 10:", task10());
