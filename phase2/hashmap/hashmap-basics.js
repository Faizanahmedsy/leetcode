/**
 * TASK 1: Count frequency of each character in a string.
 * Example: "banana" -> { b:1, a:3, n:2 }
 */
function task1(str = "banana") {}
console.log(task1());

/**
 * TASK 2: Check if a string contains any duplicate characters.
 * Example: "abc" -> false
 * Example: "aba" -> true
 */
function task2(str = "aba") {}
console.log(task2());

/**
 * TASK 3: Return the first non-repeating character in a string.
 * Example: "leetcode" -> "l"
 * If none exists, return null.
 */
function task3(str = "leetcode") {}
console.log(task3());

/**
 * TASK 4: Check if two strings are anagrams of each other.
 * Example: "listen", "silent" -> true
 */
function task4(str1 = "listen", str2 = "silent") {}
console.log(task4());

/**
 * TASK 5: Count frequency of numbers in an array.
 * Example: [1,2,2,3,3,3] -> {1:1, 2:2, 3:3}
 */
function task5(arr = [1, 2, 2, 3, 3, 3]) {}
console.log(task5());

/**
 * TASK 6: Check if all characters in a string occur the same number of times.
 * Example: "abacbc" -> true
 * Example: "aaabb" -> false
 */
function task6(str = "abacbc") {}
console.log(task6());

/**
 * TASK 7: Find the element that appears only once in an array.
 * Example: [2,2,1] -> 1
 */
function task7(arr = [2, 2, 1]) {}
console.log(task7());

/**
 * TASK 8: Group words that are anagrams.
 * Example: ["eat","tea","tan","ate","nat","bat"]
 * Output: [["eat","tea","ate"],["tan","nat"],["bat"]]
 */
function task8(words = ["eat", "tea", "tan", "ate", "nat", "bat"]) {}
console.log(task8());

/**
 * TASK 9: Check if a sentence is a pangram.
 * (Contains every letter from 'a' to 'z')
 * Example: "thequickbrownfoxjumpsoverthelazydog" -> true
 */
function task9(sentence = "thequickbrownfoxjumpsoverthelazydog") {}
console.log(task9());

/**
 * TASK 10: Two Sum using hashmap.
 * Return indices of two numbers that add up to target.
 * Example: nums = [2,7,11,15], target = 9 -> [0,1]
 */
function task10(nums = [2, 7, 11, 15], target = 9) {}
console.log(task10());
